export const environment = {
  production: false,                    // switch to true in environment.prod.ts
  api: 'http://localhost:3000',         // ← your back‑end origin
  withCreds: true,                      // ← tell HttpClient to send cookies
};
